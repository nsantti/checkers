class GamePiece {
  constructor(owner) {
    this.owner = owner;
  }

}

GamePiece.prototype.showLegalMoves = function(square, board) {
  fill(145);
  //ellipse(30, 30, this.size, this.size);
}
